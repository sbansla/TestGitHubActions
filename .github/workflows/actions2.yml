name: page-2
run-name: ${{ github.actor }} page 2
on:
  workflow_dispatch
jobs:
  job1:
    name: Running job1
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: run child process
        run: |
          node .github/scripts/test_child_process.js
      
  job2:
    name: Running job2
    needs: [job1]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Step1 from job2
        run: |
          echo "Ending job2"
        